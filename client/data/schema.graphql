type Mutation {
  createRecord(title: String, content: String): Record
  deleteRecord(_id: ID): ID
  updateRecord(_id: ID, title: String, content: String): Record
}

type Query {
  records: [Record]
}

type Record {
  _id: ID
  title: String
  content: String
  dateCreated: String
  dateModified: String
}
